name: 'Extract Commit Action'
description: 'Extracts information from commit messages.'
inputs:
  commit_limit:
    description: 'Number of commits to retrieve.'
    required: true
  extract_command:
    description: 'Command to extract information from commit messages.'
    required: false
  pretty:
    description: 'Whether to use pretty format for git log.'
    required: false
    default: 'false'
  output_variable:
    description: 'The name of the output variable to set.'
    required: true
    default: 'ENVIRONMENT'
outputs:
  environment:
    description: 'Extracted environment information.'
runs:
  using: 'docker'
  image: 'Dockerfile'
  env:
    INPUT_COMMIT_LIMIT: ${{ inputs.commit_limit }}
    INPUT_EXTRACT_COMMAND: ${{ inputs.extract_command }}
    INPUT_PRETTY: ${{ inputs.pretty }}
    INPUT_OUTPUT_VARIABLE: ${{ inputs.output_variable }}